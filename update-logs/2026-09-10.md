# 2026-09-10 更新记录

> 归档时间：2026-09-10 14:45

- 修复 AI 文档识别失败：当前自定义供应商使用推理模型，原直连 `chat/completions` 时输出预算全部被 `reasoning_content` 占用，正式 `content` 为空，前端因此提示“AI 未返回内容”。
- 文档提取恢复优先调用本机 Codex CLI 的最终回答，并关闭高推理档位，显著降低空返回和超时风险；CLI 不可用时仍保留直连模型的扩容重试兜底。
- 本机 AI 服务的鉴权读取支持 `ALLKNOW_AI_TOKEN`、`DEEPSEEK_API_KEY`、`OPENAI_API_KEY`、`~/.codex/config.toml` 与 `~/.codex/auth.json`，适配当前 Codex 配置切换。
- 收紧文档提取提示词：只保留重点操作步骤、操作内容和对应备注，过滤执行情况、是/否、图片信息、页眉页脚、元信息等噪声；备注合并到对应步骤同一行。
- 增强本地兜底解析：表格行即使无法调用 AI，也会保留步骤编号、操作内容和备注，不再只返回步骤名称。
- 已用真实 `1.1投产机柜上架.xlsx` 样例验证：正确输出标题、5 条步骤及每步同行备注；`/ping`、`/extract`、`/ask` 均通过测试，`npm run build` 通过。

## 追加：修复步骤排序并细化智能提取（2026-09-10 二次更新）

- 文档提取改为 JSON Schema 结构化输出，AI 分别返回 `number`、`name`、`action`、`remark` 四个字段，避免模型直接排版时遗漏或混淆内容。
- 服务端新增顺序校验：全部步骤带数字编号时按编号自然升序排列，混合或无编号文档按原文从上到下、从左到右的顺序重排，禁止模型按重要性或相似度调换步骤。
- 细化步骤提取规则：一个步骤中的多个关键操作按原文顺序完整保留，表格中的操作名称、操作内容和备注列分别识别，备注字段只保留对应注意事项、风险、禁止和参照信息。
- 使用结构化样例回归验证，标题、步骤编号、操作内容、备注和输出顺序均符合预期。

## 追加：AI 自动配置与连接自愈（2026-09-10 三次更新）

- 设置页新增“本机 AI 已自动接入”状态卡，展示当前模型、文档/表格提取、重点步骤备注和知识库问答能力，并支持一键重新检测。
- 备用云端 API 和语音/图片接口明确标记为可选配置；TXT、Markdown、Word、Excel、PDF 与知识问答默认使用本机 AI，无需填写 Key。
- 本机 AI 服务现在每次健康检查和问答前自动重新读取 Codex 配置、环境变量与 `~/.codex/auth.json`，模型或鉴权变化后无需重启服务。
- 新增连接自愈：检测到 `127.0.0.1` 本地代理使用 `PROXY_MANAGED` 等无效令牌时，自动切换为已验证可用的 DeepSeek 直连，避免状态显示在线但请求返回 401。
- 文档提取标题增加文件名兜底，模型误返回“文档/附件”等泛化标题时自动使用真实文件名。
- 回归验证 `/ping`、`/ask`、`/extract`：模型识别为 `deepseek-v4-flash`，健康检查返回 `extract`、`ask` 能力，问答与结构化步骤提取均成功。

## GitHub 同步

- 本日全部知识库界面、AI 文档识别、步骤排序、结构化提取、AI 自动配置与连接自愈改动统一提交到 `main`。
- 同步目标：`https://github.com/vcnn5fjbf6-cell/DC-.git`。

## 追加：文档库增加设施相关二级分类（2026-09-10 四次更新）

- “文档库”页面顶部新增“二级文档库”区域，固定展示“交付相关文档库”和“设施相关文档库”。
- 二级分类支持直接点击进入，并显示该分类下的条目数量。
- 修正文档库说明文案，明确该页面同时包含二级分类和用户新建的知识条目。
- `npm run build` 构建通过，准备同步到 GitHub `main` 分支。

## 追加：修复 AI 提取无效果与响应慢（2026-09-10 五次更新）

- 定位到文档提取原默认使用 `deepseek-v4-flash` 推理模型，单次请求需约 15.7 秒，并可能因推理内容占满输出预算而返回空正文。
- 本机 AI 服务在本地代理鉴权失效时，改为自动切换到 `deepseek-chat` 快速直连模型；文档提取关闭推理过程并使用 `response_format: json_object` 输出结构化结果。
- 提取参数调整为 `temperature: 0`、`maxTokens: 6000`，减少无效思考、重复输出和等待时间。
- 使用 `1.1投产机柜上架.xlsx` 真实样例连续回归三次，响应时间分别为 1.31 秒、1.41 秒、1.17 秒，标题、步骤编号、重点操作步骤和对应备注均稳定输出。
- `/ping` 验证模型已切换为 `deepseek-chat`，服务端点为本机 AI 可用的 DeepSeek 直连；`npm run build` 构建通过。
- 本次提速与稳定性修复同步提交并推送到 GitHub `main` 分支。

## 追加：AI 提取隐藏备注标签（2026-09-10 六次更新）

- AI 仍会读取每一步对应的备注、注意、风险、参照、禁止或避免事项，并用于补全步骤语义。
- 最终展示格式不再输出“备注：”标签，也不再生成独立“备注”分区。
- 表格提取中的备注、注意或风险内容改为直接并入对应步骤正文，保留内容但隐藏备注字段名称。
- 本地结构化降级路径同步移除独立备注区；设置页能力文案由“重点步骤与备注”调整为“重点步骤与补充内容”。
- `npm run build` 与真实附件提取回归通过后同步到 GitHub `main` 分支。

## 追加：新增长文 AI 润色（2026-09-10 七次更新）

- 本机 AI 服务新增 `/polish` 接口，使用 `deepseek-chat` 快速模型处理长文和混乱排版。
- AI 润色会删除重复内容、乱码、页眉页脚和模板套话，合并错误换行，统一标题、步骤、列表与标点表达。
- 润色过程严格保留事实、数字、日期、步骤编号、步骤顺序、专有名词、链接和代码，不编造原文没有的信息。
- 备注、注意、风险、禁止和参照信息统一并入对应步骤正文，不显示“备注”字样或独立备注区。
- 条目编辑器新增“AI 润色”按钮，执行前二次确认，完成后自动切换到预览；超长正文按约 12000 字边界自动分段润色，再依次合并，避免截断丢失内容。
- 本机 AI 能力状态新增“长文润色整理”；`/ping` 返回 `extract`、`polish`、`ask` 三项能力。
- 真实乱格式样例验证：AI 润色约 1.3–4.3 秒返回，自动合并为连续编号步骤，输出中“备注”出现次数为 0；`npm run build` 构建通过。
- 已通过真实接口、TypeScript 与生产构建验证，随后同步到 GitHub `main` 分支。

## 追加：润色并入自动提取流程（2026-09-10 八次更新）

- 移除条目编辑器的独立“AI 润色”按钮，不再需要用户手动执行第二次操作。
- 删除前端独立润色调用和本机 AI `/polish` 接口，AI 能力恢复为 `extract` 与 `ask`。
- 将长文润色规则直接合并到 `/extract` 自动提取流程：自动删除重复词、口水话、乱码、错误换行和无效空行，合并重复操作或补充信息，统一标点、句式和专业表达。
- 润色仍严格保留事实、数字、日期、步骤编号、步骤顺序和专有名词，不编造、不增删关键信息。
- 最终输出继续保持不显示“备注”标签和独立备注区，备注、注意或风险内容仅用于补全对应操作步骤。
- 服务端增加自动去重与容错：合并重复步骤、清理模型误带出的“第X步”、过滤重复片段，并在编号不连续时重新生成连续步骤编号。
- 服务端统一清理重复标点，真实混乱样例在约 1.18 秒内返回连续步骤，输出中“备注”出现次数为 0。
- 设置页能力文案调整为“重点步骤、补充内容与自动润色”，`npm run build` 与真实附件提取回归通过后同步到 GitHub `main` 分支。

## 追加：AI 提取改为分类分段输出（2026-09-10 九次更新）

- 文档提取结果不再把每一步的所有内容挤在一行，改为“操作流程 → 业务分区 → 步骤 → 操作要点列表”的 Markdown 层级。
- AI 根据原文语义将步骤归入 2–8 个清晰分区，例如“工单接收”“信息确认”“上架实施”，保持原业务流程顺序，不调换步骤。
- 每个步骤编号和名称独立成标题，操作内容、补充信息、注意或风险事项拆分为清晰要点，阅读效果优于原始表格行列。
- 增加分区内近义去重和步骤去重：同一操作、重复句子、被错误拆出的“第X步”会自动合并，编号不连续时自动重排为连续步骤。
- 设置页能力文案更新为“分类分段、重点步骤与自动润色”；真实标准表格与乱格式样例均通过，乱内容响应约 1.59 秒，输出中“备注”出现次数为 0。

## 追加：支持文档和视频页面预览（2026-09-10 十次更新）

- 附件列表中的视频现在可直接在内嵌播放器中播放，支持播放、暂停、音量和全屏控制。
- PDF 使用内嵌页面预览；Word（DOCX）通过结构化 HTML 预览；Excel、XLS、CSV 和 ET 渲染为可滚动表格。
- TXT、Markdown 和 JSON 直接以等宽文本方式预览，长内容在预览区内滚动，不影响编辑器主页面。
- 文档预览区支持多工作表、横向滚动和大表格限行显示；无法解析的格式会显示下载提示，避免误以为附件损坏。
- 视频显示高度调整为 220px，文档预览区为 320px，预览关闭后自动释放浏览器 Blob URL。
- `npm run build` 构建通过，准备同步到 GitHub `main` 分支。

## 追加：文档库条目按所属分类展示与搜索（2026-09-10 十一次更新）

- 修复二级文档库被当作普通编辑器打开的问题，“交付相关文档库”和“设施相关文档库”现在进入独立库内页面。
- 库内页面展示该文档库及其子层级下的全部条目，包含标题、正文摘要、状态和最近更新时间。
- 库内搜索范围严格限定当前文档库，可搜索标题、正文和标签，不会混入其他文档库条目。
- 在交付相关文档库内新建条目时自动设置父级为交付相关文档库；设施相关文档库同样处理，返回后即可在所属库内看到。
- 文档库卡片和二级分类条目数量改为统计全部下级条目，不再只统计直接子项。
- `npm run build` 构建通过，准备同步到 GitHub `main` 分支。

## 追加：修复文档库新建条目无反应（2026-09-10 十二次更新）

- 修复文档库落地页顶部“新建条目”仍调用全局新建的问题：在交付或设施文档库内会强制把 parentId 设置为当前文档库。
- 库内底部新建按钮在名称为空时不再静默返回，自动使用“未命名文档条目”，随后仍可编辑标题。
- 顶部与库内两个新建入口现在统一走当前文档库上下文；新建完成后返回即可在所属文档库看到该条目。
- `npm run build` 构建通过，准备同步到 GitHub `main` 分支。

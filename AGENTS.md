# Repository Instructions

- README 是项目的自述文件。
- 每次上传或更新自述文件时，必须在 README 顶部保留并更新“最后更新”字段为上传当日的 `YYYY-MM-DD` 日期。
- 日期以本地当天日期为准，不要沿用旧日期。
- 每次同步 GitHub 时，必须使用 `update-logs/YYYY-MM-DD.md` 保存当天更新记录。
- 不同日期的更新必须写入各自日期的独立文件，禁止用当天文件覆盖其他日期文件。
- 同一天多次同步时，只向当天文件追加新记录，不删除当天已写入的历史记录。
- 同步完成后，当天更新记录文件与代码一起提交并推送。
